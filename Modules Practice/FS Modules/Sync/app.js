const path = require("path")
const fs = require("fs")

const fileName = "test.txt";
const filePath = path.resolve("/Users", "Haris","Desktop","Test");
// fs.mkdirSync(filePath, {recursive: true});

// fs.writeFileSync(path.join(filePath, fileName), "I am new File", "utf-8");
// console.log(fs.readFileSync(path.join(filePath,fileName), "utf-8"))

// fs.appendFileSync(path.join(filePath, fileName), " Updates", "utf-8");

